<p style="color: green"><%= notice %></p>

<%= render @entity %>

<div>
  <%= link_to "Edit this entity", edit_group_entity_path(params[:group_id], @entity) %> |
  <%= link_to "Back to entities", group_entities_path(params[:group_id]) %>

  <%#= button_to "Destroy this entity", group_entity_path(params[:group_id], @entity), method: :delete %>
</div>
